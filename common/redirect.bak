if (Meteor.isServer) {
  REDIRECT_URL = 'http://www.redbutterfly.com.br/';

  WebApp.connectHandlers.use(function (req, res, next) {
    res.writeHead(301, {
      Location: REDIRECT_URL
    });

    res.end();
  });
}